name: Setup Java

on:
  workflow_dispatch #manual trigger key

jobs:
  setup-java:
    runs-on: ubuntu-latest
    steps:
       - name: Setup Java JDK
         uses: actions/setup-java@v3.14.1
         with:
           java-version: '17'
           distribution: microsoft
      
